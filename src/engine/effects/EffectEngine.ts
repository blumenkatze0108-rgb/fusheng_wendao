import type {Effect,SaveGame} from '../../types/game';
export function applyEffects(save:SaveGame,effects:Effect[]):SaveGame{const s=structuredClone(save) as SaveGame;for(const e of effects){if(e.type==='stat'){const v=(s.player.stats[e.stat]??0)+e.amount;s.player.stats[e.stat]=Math.max(0,Math.min(e.stat==='spiritStones'?999999:1000,v))}else if(e.type==='item'){s.inventory[e.itemId]=Math.max(0,(s.inventory[e.itemId]??0)+e.amount)}else if(e.type==='flag'){s.flags[e.flag]=e.value}else if(e.type==='log'){s.log.unshift(e.text)}else if(e.type==='travel'){s.currentLocationId=e.locationId;if(!s.visited.includes(e.locationId))s.visited.push(e.locationId)}}return s}
