export class SeededRandomEngine{private state:number;constructor(seed:string){this.state=this.hash(seed)||1}private hash(s:string){let h=2166136261;for(const c of s){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0}next(){let t=this.state+=0x6D2B79F5;t=Math.imul(t^(t>>>15),t|1);t^=t+Math.imul(t^(t>>>7),t|61);return ((t^(t>>>14))>>>0)/4294967296}int(min:number,max:number){return Math.floor(this.next()*(max-min+1))+min}pick<T>(items:T[]){return items[Math.floor(this.next()*items.length)]}derive(key:string){return new SeededRandomEngine(`${this.state}:${key}`)}}
