import React from 'react';export class ErrorBoundary extends React.Component<{children:React.ReactNode},{error?:Error}>{state:{error?:Error}={};static getDerivedStateFromError(error:Error){return{error}}render(){if(this.state.error)return <main className="screen"><h2>可恢复错误</h2><p>界面遇到异常，但存档不会被清空。请返回主菜单或刷新后读取自动存档。</p><pre>{this.state.error.message}</pre></main>;return this.props.children}}
